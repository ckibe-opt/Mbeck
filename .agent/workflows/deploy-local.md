---
description: Compile and run the Flutter app locally
---
# Deploy Local Flow

1. Clean the build files for a fresh start:
```bash
flutter clean
```

// turbo-all
2. Fetch dependencies:
```bash
flutter pub get
```

3. Run the application:
```bash
flutter run
```
