name: mbeck_shared
description: Shared models and logic for Mbeck Buyer and Seller apps.
version: 0.0.1
homepage: https://mbeck.com

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ">=0.19.0 <0.21.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
